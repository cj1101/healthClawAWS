{
  "name": "healthclaw-aws",
  "private": true,
  "type": "module",
  "description": "Nemoclaw Health Phase 0 contracts + validators",
  "scripts": {
    "validate:phase0": "node scripts/validate-phase0.mjs",
    "validate:phase1": "npm run validate:phase0 && python -m pytest tests -q"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
